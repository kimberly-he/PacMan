def myrange(start, end, step):
    """
        Return a list starting from the start,increase by step,ends before end.
	    """
	        
		    if end==start:
		            return list
			        else:
				        list.append(start)
					        if start+step>= end:
						            return list
							            else:
								                myrange(start+step,end,step)
										            return list

											            
												    list=[]

												     
